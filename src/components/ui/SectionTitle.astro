---
export interface Props {
  label: string;
  title: string;
  titleClass?: string;
}

const { label, title, titleClass = 'text-deep' } = Astro.props;
---

<p class="num-label uppercase mb-4">{label}</p>
<h2 class={`font-display text-3xl lg:text-4xl font-medium ${titleClass}`}>
  {title}
</h2>
